@model IEnumerable<BlogDia12.Models.Comentarios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.usuario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.usuario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCom }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCom }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCom })
        </td>
    </tr>
}

</table>
